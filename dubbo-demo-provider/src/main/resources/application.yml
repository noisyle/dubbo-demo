spring:
  application:
    name: dubbo-demo-provider
  main:
    banner-mode: 'off'
  profiles:
    active: '@activatedProperties@'

# Logging
logging:
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n'
  file: ${user.home}/logs/dubbo-demo-provider.log
  level:
    root: info
    org.springframework.web: debug
    com.noisyle: debug
